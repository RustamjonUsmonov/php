index.php is just simple form, which gets info from user
sec.php is main file with script, that gets sentences from user,
generates one more sentence foreach given, and sorts using second words of sentences
Note that during sorting for instance "F">"a"
